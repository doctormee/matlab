%%
a = -pi;
b = pi;
size = 500;
x = linspace(a, b, size);
xx = linspace(a, b, 2 .* size);
%%
f = @(x) cos(2.*x);
fDerMax = 2;
apriori = fDerMax .* (b - a) ./ size;
nearF = interp1(x, f(x), xx, 'nearest');
plot(xx, apriori, '-r', xx, nearF(xx) - f(xx), '-b
